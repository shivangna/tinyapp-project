
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
      <form action="" method="POST">

        <% if (user_id) { %>
    <% for(var KeyValue in urlDatabase) { %>

         <% if (shortURL === KeyValue){ %>
         <h3> Short URL: <%= KeyValue %> </h3>
         <h3> long URL: <%= urlDatabase[KeyValue].longURL %> </h3>
        <label for="longURL">Enter the updated Long URL:</label>
        <input type = "text" name = "longURL" value="">
        <input type="submit" name="Update">
    </form>
    <% } else { %>
    <% "Short URL does not exist. Please try again" %>
    <% } %>
    <% } %>
    <% } else { %>
    <% return "please login at localhost:8080/login to edit a URL" %>
  <% } %>
</body>
</html>